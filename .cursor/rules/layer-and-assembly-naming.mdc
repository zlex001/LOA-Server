---
alwaysApply: true
---

本项目采用分层架构，程序集命名约定如下：

- Utils：工具层（无任何依赖）
- Basic：基础层（仅依赖 Utils）
- Logic：数据与业务逻辑层（依赖 Basic）
- Net：网络层（依赖 Logic）
- Domain：领域与业务层（依赖 Net）
- Start：启动层 / 主程序入口（依赖所有核心程序集）
- Daemon：独立的守护进程项目，不参与主程序分层依赖
