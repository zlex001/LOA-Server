---
alwaysApply: true
---

# 多语言数据规则

项目中的多语言数据（即语言策划表，语言.csv）必须遵循以下规则。

## 唯一性与复用

- 多语言文本应作为"语言资源"统一管理，而不是零散文本的集合。
- 禁止因使用场景不同而重复创建内容相同的多语言条目。
- 若多个系统、模块或功能需要使用同一段文本，应复用同一条数据。
- 新增多语言内容时，应优先检查是否已存在可复用的条目。
- 为了避免语言数据膨胀与语义漂移，如果出现了未被使用的冗余数据，应当及时清理。

## 世界观包装

- 包装（Packaging）：将功能性术语转化为符合世界观的玩家可见表述，仅变更翻译列，不改变 id、cid、label 和程序逻辑。
- 内部标识（id, cid, label）保持功能性命名，便于程序和策划理解。
- 翻译列使用包装后的名称，面向玩家展示。
- 包装应基于世界观中具体的神、人、物、事，避免抽象修辞。

## 目标

- 保证多语言数据的唯一性、可维护性与长期可扩展性。
- 降低翻译成本，避免语言数据膨胀与语义漂移。
- 通过世界观包装提升玩家沉浸感。