---
alwaysApply: true
---

# 分层架构规则

## 层定义与职责

- Utils：工具层（无任何依赖）
- Basic：基础层（仅依赖 Utils）
- Data：数据层（依赖 Basic）
- Net：网络层（依赖 Data）
- Logic：业务逻辑层（依赖 Net）
- Start：启动层 / 主程序入口（依赖所有核心程序集）
- Daemon：独立的守护进程项目，不参与主程序分层依赖

## 依赖方向

Utils → Basic → Data → Net → Logic → Start

- 只允许向下依赖，不允许任何形式的反向依赖
- 严禁跨层依赖与循环依赖
- 所有架构设计与代码实现必须遵循该依赖顺序
- 当实现需求与分层规则冲突时，必须优先调整设计，而不是绕过规则

## 反向通信机制

下层需要通知上层时，使用以下机制（均基于 Monitor 类，不违反依赖方向）：

1. **对象生命周期监听**
   - `Content.Add.Register(typeof(T), callback)` / `Content.Remove.Register(...)`
   - 适用场景：对象被添加或移除时响应

2. **自定义事件监听**
   - `monitor.Register(Event.X, callback)` / `monitor.Fire(Event.X, args)`
   - 适用场景：业务事件（点击、聊天、死亡、完成等）

3. **属性变化监听**
   - `data.before.Register(Data.X, callback)` / `data.after.Register(...)`
   - 适用场景：属性变化前拦截或变化后响应

4. **委托注入（次要方式）**
   - 下层定义 `public static Func<...>` 属性，上层在 Init 时设置实现
   - 适用场景：下层需要"询问"上层的能力（如条件检查）

设计原则：优先使用观察者模式（1-3），状态变化时应在变化发生时处理，而不是事后追溯判断。

## 事件作用域

基于 Monitor 的事件可以在不同作用域触发：

| 作用域 | 触发方式 | 适用场景 |
|-------|---------|---------|
| 对象实例 | `obj.monitor.Fire(Event.X, ...)` | 事件只与该对象直接相关，监听方是该对象的关联者 |
| 管理单例 | `Agent.Instance.monitor.Fire(Event.X, ...)` | 多个无关系统需要响应，或事件具有全局意义 |

选择原则：

1. **默认使用对象实例**：事件在发生变化的对象上触发
2. **提升到管理单例的条件**：
   - 多个独立系统需要监听同一事件
   - 监听方无法在事件发生前获取对象引用
   - 事件具有跨系统的业务意义（如玩家点击、玩家死亡）
3. **双触发模式**：同一事件可同时在两个作用域触发
   - 对象实例：供直接关联方响应
   - 管理单例：供全局系统响应（如教程、成就、统计）
