---
alwaysApply: true
---

# 分层依赖方向规则

本项目采用严格的单向分层依赖结构，依赖方向必须为：

Utils → Basic → Logic → Net → Domain → Start

规则说明：

- 只允许向下依赖，不允许任何形式的反向依赖
- 严禁跨层依赖与循环依赖
- 所有架构设计与代码实现必须遵循该依赖顺序
- 当实现需求与分层规则冲突时，必须优先调整设计，而不是绕过规则
